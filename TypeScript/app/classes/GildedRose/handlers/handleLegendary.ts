import CustomItem from "../../CustomItem";

const handleLegendary = (item: CustomItem) => {
    // Placeholder for any future needs re: legendary items
    return item;
};

export default handleLegendary;
